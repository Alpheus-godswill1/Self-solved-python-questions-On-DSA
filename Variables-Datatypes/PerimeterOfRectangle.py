# Create a Python program that calculates the perimeter of a rectangle using variables for length and width.

""""
P = 2 × (length + width)

Where:

P is the perimeter of the rectangle.
length = the length of the rectangle.
width = the width of the rectangle.

"""
Length = float(input("Input the length of your rectangle: "))
Width = float(input("Input the width of your rectangle: "))
Perimeter = 2 * (Length + Width)
print(Perimeter)


