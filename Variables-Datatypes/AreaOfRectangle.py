# Create a Python program to calculate the area of a rectangle using variables for length and width.

Length = float(input("Please enter the length of the Rectangle here: "))
Width = float(input("Please enter the width of the Rectangle here: " ))

# Print the Value
print(f"The Area of the Rectangle is = {Length * Width}")
